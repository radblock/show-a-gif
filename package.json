{
	"name": "gimme",
  "description": "file upload page with lambda and s3",
  "private": true,
  "files": [
    "main.js"
  ],
  "scripts": {
    "start": "claudia create --name random-art-gif --region us-east-1 --handler main.handler",
    "test": "claudia test-lambda",
    "deploy": "claudia update"
  },
  "dependencies": {
    "aws-sdk": "2.3.5"
  },
  "devDependencies": {
    "claudia": "^1.2.x"
  }
}

